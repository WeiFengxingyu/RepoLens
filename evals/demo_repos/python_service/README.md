# Python Service Demo

Small FastAPI-style backend used by RepoLens Phase 5 demos.

It includes API routes, service classes, repository classes, auth validation,
billing logic, scanner filters, chunk building, audit logging, retry helpers,
and tests. The code is intentionally compact and synthetic; it contains no real
secrets or customer data.
